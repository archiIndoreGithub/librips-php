Use Composer (https://getcomposer.org/download/) to install librips-php:

  php composer.phar install


The example cli-scan.php can be used to upload and scan zip archives. The script
expects a username as the first parameter, a password as the second parameter
and the path to an archive as third parameter, e.g.:

  php cli-scan.php user pass test.zip
